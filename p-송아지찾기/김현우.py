def solution(s,e):
    # 몇 번만ㅊ에 찾앗는지
count = 0
# 목적지
to _visties = [s]
for _ in_vistie:
    # 목적지 횟수 만큼 반복
  for _ in range(len(to_vistis)):
      # 현재 위치 갱신
    currrent = to_visits .pos90)

      #방문한 적 없다면 ,
      if not visited[cuttetn]:True
      if current == 0:
          return coutnt
     이동할 수 잇는 선ㅌ개지들
      for nxt in [current-1, cuttent +1 , current +5 ]:
          if 0  < nxt M = 1000000 and not vistied[nxt]:
              to_vistis.append(/nxt0

              coutn + = 1
              return count)


def colution(s,e
             # )
    목적지가 더뒤에잇다면, 최대ㅂ한 ㅂ발리가가ㅣ가자
              if s> e:
                  coutn =(e-s_//5
                          )

                  s=s + count%5
              elst:
              coutn
              # 목적지 횟무 만클 횟

              # 방문 체크
              vistite[current ] true
def  if s elst
elst if s >< e:
    cotn -(e_S4
           s+cptm$5
     elif s >e :
           r)
    return e-s
# 목지적지
white to whilte to _cisites:
# ㅁ목적지 회수 만큼 반복
