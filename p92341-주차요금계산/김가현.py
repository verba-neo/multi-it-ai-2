# fees : 주차 요금을 나타내는 정수 배열 [기본시간(분), 기본요금, 단위시간(분), 단위요금]
# records : 자동차의 입/출차 내역을 나타내는 문자열 배열 ["시각 차량번호 내역"]
# 차량 번호가 작은 자동차부터 청구할 주차 요금을 차례대로 정수 배열에 담아서 반환하는 함수

# strptime : 문자열을 datetime 객체로 변환

def solution(fees, records):
    fees
    datetime.strptime(records[])
    # records의 각 문자열을 리스트로 변환
    for i in range(0, len(records)):
        info[i] = list(records[i].split())


    answer = []
    return answer