sys format(f)
ftxt

# 1260 dfs/bfs
from collections import deque
#n: 저엄의 개수, m : 간서의 개수 v: 탐색을 시작할 ㅓㄴ호
n, m, v = map(int,impourt()).split()))

grap = [[ for_ in range(n_1]]0)]]

for i in range()
    a,b =map (in .imput () spliee)))
    grapsh [a].  split()))
    graoh a
    appendb
    graph b appenda
    graph [a] .rot)
    graphb)_

    def dfs(grapgh , b, bistie:
            #)
    # 현재 노드르 방문처리
    vistied[v] ture
pritn vendl
# 현재 노드아ㅗ 연결된 다른 노드를 재귀적으로 바움ㄴ
for j in graph[v]
    if not visted[f]
    dfe(grapgh v jf jvistedd)

    #bds 함수 정의
    # )

    def bfs9graph, start, vistiedd):
# 큐
queeue =deque([start])

visited[staret] true
# 큐가 빌 떄까지 바녹
whitle queue :
 # v -queeu .poleft()
pritn(vbm end l''
pritn (v ,end -;
#해당 원소와 연결된 ,아직))
pritn 8v,end''
for k in graph[v]'' \
        'kif p tvidiged[ke]' \
        'queuw.append{'